#include "main.h"
/**
 * reset_to_90 - takes a pointer to an int parameter and updates the value it points to 90
 *
 * on: int parameter
 * Return: Nothing
 */

int _putchar(char c)
{
	return (write(1, &c, 1));
}
